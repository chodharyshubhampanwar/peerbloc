import React from "react";

const Button = () => {
  return (
    <button className="bg-primary text-primary-foreground px-4 py-2 rounded-md hover:bg-primary-foreground hover:text-primary transition-colors">
      Sign In
    </button>
  );
};

export default Button;
